#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cstring>
#include <string>
#include <cmath>
#include <queue>
#include <map>
#include <set>
#include <vector>
using namespace std;

long long n, m;
int num1[200];
int len = 0;

void check(long long x,int judge){
    int p = 0;
    int val = 0;
    while(x > 0){
        x /= 7;
        p++;
    }
}

int main( ){
    cin>>n>>m;
    check(n,0);
    check(m,1);
    return 0;
}
